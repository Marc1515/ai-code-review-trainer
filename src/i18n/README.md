# i18n

next-intl configuration and message catalogs. **Planned as the next step,
before the review feature is implemented:** the request config, locale routing
(`[locale]` segment) and `messages/{es,en}.json`.

- Primary UI language: **Spanish (`es`, default)**
- Secondary UI language: **English (`en`)**

Code, comments and technical docs stay in English.
