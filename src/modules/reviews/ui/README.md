# reviews / ui

React components for the reviews feature (form + result view). Phase 1 adds the
paste textarea, review-type selector, and the structured result display built
from Shadcn primitives (`@/components/ui`). Components call the Server Action in
`@/server/actions`; they contain no AI or persistence logic.
